---
import '@fontsource/inter';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="ru" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="bg-background text-white min-h-screen">
    <header class="border-b border-gray-800">
      <nav class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <a href="/" class="text-2xl font-bold text-accent-orange">🥇 ТОП Стриминговых Сервисов</a>
          <div class="flex gap-6">
            <a href="/" class="hover:text-accent-orange transition-colors flex items-center gap-2">
              <span>🎬</span>
              <span>Сервисы</span>
            </a>
            <a href="/comparison" class="hover:text-accent-orange transition-colors flex items-center gap-2">
              <span>📊</span>
              <span>Сравнение</span>
            </a>
            <a href="/reviews" class="hover:text-accent-orange transition-colors flex items-center gap-2">
              <span>⭐</span>
              <span>Отзывы</span>
            </a>
          </div>
        </div>
      </nav>
    </header>
    <main class="container mx-auto px-4 py-8">
      <slot />
    </main>
  </body>
</html>

<style is:global>
  html {
    font-family: 'Inter', system-ui, sans-serif;
  }
</style>